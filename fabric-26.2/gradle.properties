# Ping to Map - Fabric 26.2
org.gradle.jvmargs=-Xmx3G
org.gradle.daemon=true
org.gradle.parallel=true
org.gradle.caching=true

# ─── Minecraft / Fabric ─────────────────────────────────────────
minecraft_version=26.2
loader_version=0.19.3
fabric_version=0.155.2+26.2

# ─── Mod metadata ──────────────────────────────────────────────
mod_id=pingtomap
mod_name=Ping to Map
mod_license=MIT
mod_version=1.2.0
mod_group_id=com.kuronami.pingtomap
mod_authors=KURONAMI
mod_description=Pop a temporary JourneyMap waypoint when a teammate or yourself pings via Ping-Wheel (Fabric 26.2).

# ─── Required / Optional dependencies (Modrinth version_id) ─────
# Modrinth version_id 指定（jar 共有形式のため version_number では取れない）。
# PLAN_MAP_26X.md Wave 0d の 26.2-fabric 列 ＋ 本セルで新規解決した journeymap/common-network
# Ping-Wheel Fabric 26.2 (release)
ping_wheel_version_id=lsH1el9E
# Forge Config API Port for Fabric 26.2 (ForgeConfigSpec を提供)
fcap_version=rSd3GiG8
# JourneyMap 本体 (Fabric 26.2、runClient テスト用。Modrinth API を game_versions=["26.2"]&loaders=["fabric"] で新規解決)
jm_version=gNAWSuoC
# JourneyMap Fabric の実行時依存 (Common Network by mysticdrew、Fabric 26.2 で新規解決)
common_network_version=iXO2jgbi
